{
  "name": "website-uptime-monitor-action",
  "version": "1.0.5",
  "description": "Checks a list of URLs, logs status/response times, opens an issue on failures, and commits the log file.",
  "main": "index.js",
  "scripts": {
    "build": "npx ncc build src/index.js -o dist",
    "test": "vitest",
    "test:watch": "vitest --watch"
  },
  "dependencies": {
    "@actions/core": "^2.0.2",
    "@actions/exec": "^3.0.0",
    "@actions/github": "^9.0.0"
  },
  "devDependencies": {
    "@vercel/ncc": "^0.38.4",
    "vitest": "^4.0.18"
  }
}
